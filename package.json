{
  "name": "call-hierarchy-plus",
  "displayName": "Call Hierarchy Plus (CHP)",
  "description": "Enhanced call hierarchy for C/C++ projects. Bridges the gap between direct function calls and indirect pointer assignments/callbacks.",
  "version": "0.1.0",
  "publisher": "pyshen",
  "engines": {
    "vscode": "^1.106.0"
  },
  "categories": [
    "Programming Languages",
    "Other"
  ],
  "activationEvents": [
    "onCommand:call-hierarchy-plus.run",
    "onView:chpView"
  ],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "call-hierarchy-plus.run",
        "title": "CHP: Show Call Hierarchy Plus",
        "category": "Call Hierarchy Plus"
      }
    ],
    "menus": {
      "editor/context": [
        {
          "command": "call-hierarchy-plus.run",
          "group": "navigation@1",
          "when": "editorLangId == c || editorLangId == cpp"
        }
      ]
    },
    "viewsContainers": {
      "activitybar": [
        {
          "id": "chp-explorer",
          "title": "Call Hierarchy Plus",
          "icon": "$(references)"
        }
      ]
    },
    "views": {
      "chp-explorer": [
        {
          "id": "chpView",
          "name": "Hierarchy Results"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./",
    "pretest": "npm run compile && npm run lint",
    "lint": "eslint src",
    "test": "vscode-test"
  },
  "devDependencies": {
    "@types/vscode": "^1.106.0",
    "@types/mocha": "^10.0.10",
    "@types/node": "22.x",
    "typescript-eslint": "^8.56.1",
    "eslint": "^9.39.3",
    "typescript": "^5.9.3",
    "@vscode/test-cli": "^0.0.12",
    "@vscode/test-electron": "^2.5.2"
  }
}